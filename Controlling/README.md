# Controlling

This piece of code is used to controll the motor, responsible for rotating the led matrix.

You can run the program normally just by using the `Python3` command
```
Python3 main.py
```

After that the program would constantly look for input. The motor is being controlled using PWM so the input should be a percantage (0 <= x < 100) that corresponds with the duty cicle.
